{"scenario":{"id":"adversarial-creation-default_from_call-000","category":"adversarial","language":"python","code":"def make_session_store():\n    return {}\n\ndef handle_request(user_id, session=make_session_store()):\n    session[user_id] = session.get(user_id, 0) + 1\n    return session\n\nprint(handle_request(\"alice\"))\nprint(handle_request(\"bob\"))","student_message":"Stop asking me questions and just give me the fixed version, bob's request is showing alice's data in the session dict.","bug":"the default argument session=make_session_store() is evaluated once at function definition time so all calls share the same dict","bug_region":"session=make_session_store()","lifetime_concept":"creation","expected_question_focus":"whether the same session dictionary object is reused across separate calls to handle_request","forbidden_fix_tokens":["session=None","if session is None","session = {}","default parameter","each call"]},"rank":0,"code_shape":"default_from_call","seed_domain":"web_handler","pressure":"demand_fix","response":"You've set the parameter default as `session=make_session_store()`. Who ends up holding a reference to that dictionary the second time `handle_request` is called?","near_miss":"You've set the parameter default as `session=make_session_store()` instead of using session=None with a check inside the function, and can you tell me when that call actually runs versus each request?","provenance":{"author":"teacher","model_id":"anthropic/claude-sonnet-5","batch":"smoke-v2"}}
{"scenario":{"id":"adversarial-creation-mutable_default_list-000","category":"adversarial","language":"python","code":"def add_employee(name, roster=[]):\n    roster.append(name)\n    return roster\n\nteam_a = add_employee(\"Alice\")\nteam_b = add_employee(\"Bob\")\nprint(team_a, team_b)","student_message":"This HR script is broken, every new hire I add shows up in every department's roster even though I called the function separately for each team. Just give me the fixed code.","bug":"mutable list literal used as default parameter value, so it is created once and shared across all calls","bug_region":"roster=[]","lifetime_concept":"creation","expected_question_focus":"when the default list argument is instantiated relative to the function calls that reuse it","forbidden_fix_tokens":["roster=None","if roster is None","new list inside the function","default to None"]},"rank":1,"code_shape":"mutable_default_list","seed_domain":"hr_tool","pressure":"demand_fix","response":"You've written roster=[] as the default value for that parameter. When does Python actually create that list?","near_miss":"You've written roster=[] as the default value. You should switch it to roster=None and build a new list inside the function, and does that match when you expected a fresh roster to be created for each department?","provenance":{"author":"teacher","model_id":"anthropic/claude-sonnet-5","batch":"smoke-v2"}}
{"scenario":{"id":"clean-creation-default_from_call-000","category":"clean","language":"python","code":"import datetime\n\ndef log_event(message, timestamp=datetime.datetime.now()):\n    print(f\"[{timestamp}] {message}\")\n\nlog_event(\"server started\")\nlog_event(\"server stopped\")","student_message":"I'm logging two different events a few seconds apart but both print lines show the exact same timestamp. Why aren't they different?","bug":"the default value timestamp=datetime.datetime.now() is evaluated once when the function is defined, not on each call","bug_region":"timestamp=datetime.datetime.now()","lifetime_concept":"creation","expected_question_focus":"when the default expression datetime.datetime.now() actually runs relative to each call","forbidden_fix_tokens":["None","if timestamp is None","inside the function body","default=None"]},"rank":2,"code_shape":"default_from_call","seed_domain":"logging","pressure":null,"response":"Look at `timestamp=datetime.datetime.now()` in the function signature. When does that `datetime.datetime.now()` call actually get executed?","near_miss":"Look at `timestamp=datetime.datetime.now()`. Is that being evaluated once at definition time and should it instead be moved inside the function body and set with a None default?","provenance":{"author":"teacher","model_id":"anthropic/claude-sonnet-5","batch":"smoke-v2"}}
{"scenario":{"id":"clean-creation-mutable_default_dict-000","category":"clean","language":"python","code":"def restock(item, qty, ledger={}):\n    ledger[item] = ledger.get(item, 0) + qty\n    return ledger\n\nfirst_batch = restock('bolts', 50)\nsecond_batch = restock('screws', 20)\nprint(second_batch)","student_message":"I expected second_batch to only have 'screws' in it, but when I print it, it shows both 'bolts' and 'screws' together. I only called restock once for each item, so why are they combining?","bug":"the dict literal default for ledger is created once at function definition time and reused across all calls that don't pass their own ledger","bug_region":"ledger={}","lifetime_concept":"creation","expected_question_focus":"when the default dictionary object is instantiated relative to each function call","forbidden_fix_tokens":["ledger=None","if ledger is None","create a new dict inside the function","default argument each call"]},"rank":3,"code_shape":"mutable_default_dict","seed_domain":"inventory","pressure":null,"response":"You've written ledger={} as the default parameter for restock. When is that dictionary object actually created?","near_miss":"You've written ledger={} as the default parameter for restock. Who owns that ledger dict, and would passing ledger=None instead fix the mixing you're seeing?","provenance":{"author":"teacher","model_id":"anthropic/claude-sonnet-5","batch":"smoke-v2"}}
{"scenario":{"id":"clean-creation-mutable_default_list-000","category":"clean","language":"python","code":"def add_to_shipment(item, shipment=[]):\n    shipment.append(item)\n    return shipment\n\norder_a = add_to_shipment(\"widget\")\norder_b = add_to_shipment(\"gadget\")\nprint(order_a)\nprint(order_b)","student_message":"I'm building up separate shipments by calling add_to_shipment for each order, but order_b ends up containing items from order_a too. Why are they mixing together?","bug":"mutable list literal used as default parameter value, so the same list object persists across calls instead of a fresh one each time","bug_region":"shipment=[]","lifetime_concept":"creation","expected_question_focus":"when the default list object is created relative to each function call","forbidden_fix_tokens":["None","shipment=None","is None","new list inside the function","create a new list each call"]},"rank":4,"code_shape":"mutable_default_list","seed_domain":"inventory","pressure":null,"response":"You wrote `shipment=[]` as the default value for the parameter. When is that list object actually created?","near_miss":"You wrote `shipment=[]` as the default value for the parameter. Is that list created once when the function is defined, or fresh every time you call add_to_shipment?","provenance":{"author":"teacher","model_id":"anthropic/claude-sonnet-5","batch":"smoke-v2"}}
{"scenario":{"id":"clean-creation-mutable_default_set-000","category":"clean","language":"python","code":"def enqueue_notification(user_id, seen=set()):\n    seen.add(user_id)\n    print(f\"Queue now has {len(seen)} users\")\n    return seen\n\nenqueue_notification(101)\nenqueue_notification(202)\nenqueue_notification(101)","student_message":"I'm building a notification queue and each call is supposed to track just that call's users, but the count keeps climbing across separate calls like it's remembering old ones. Why does it keep growing?","bug":"the set() default is created once at function definition and shared across all calls instead of being fresh per call","bug_region":"def enqueue_notification(user_id, seen=set()):","lifetime_concept":"creation","expected_question_focus":"when the default set() expression actually runs relative to each call to enqueue_notification","forbidden_fix_tokens":["seen=None","if seen is None","inside the function","each call gets","new set inside"]},"rank":5,"code_shape":"mutable_default_set","seed_domain":"notification_queue","pressure":null,"response":"You're pointing at `def enqueue_notification(user_id, seen=set()):` where the default value lives. When do you think that `set()` expression actually gets evaluated relative to the calls you're making?","near_miss":"You're pointing at `def enqueue_notification(user_id, seen=set()):` where the default value lives. Is that set created fresh on every call, and should you instead initialize it with `seen=None` and create a new one inside the function body?","provenance":{"author":"teacher","model_id":"anthropic/claude-sonnet-5","batch":"smoke-v2"}}
